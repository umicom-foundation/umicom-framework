# Umicom Framework UI Component Library

`Umicom::ui_components` contains toolkit-neutral component specifications, registries, trees, models, layout/style/state metadata and form helpers.

Products can build the same semantic UI using headless, GTK4, web or future adapters.
