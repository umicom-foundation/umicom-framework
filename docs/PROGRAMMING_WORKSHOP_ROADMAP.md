# Programming Workshop — Delivery and next tracks

Framework owns curriculum data, lesson resources, safe practice descriptions and learning contracts. Studio and Education are consumers.

## Delivered in this batch

- 20 original catalogue entries retained without field or order changes.
- One integrated Git workflow guide and 32 runnable programming labs appended, for 53 entries.
- Framework learning CMake integration and original install rules reused.
- A thin Education native catalogue console; no parallel curriculum.
- Existing Studio Guided Learning catalogue projection retained, with GUI smoke tests still required.

## Next steps, not yet delivered

1. Reusable lesson/resource discovery across source, staged and installed locations, including arbitrary working directories; use existing resource/location contracts, not a second locator in each application.
2. Education graphical lesson reading through the shared Framework reader and existing application shell; retain the native console as a diagnostic path.
3. Approved exercise execution through the existing task/process/workspace permission contracts: scratch workspace, output limits, timeout, cancellation and explicit evidence. Never execute an HTML snippet automatically.
4. Durable learner progress and assessment through existing Framework learning and Data Server contracts, including restart and migration tests. A passing example is not automatically a learner assessment.
5. Expanded systems track: processes, virtual memory, caches, interrupts and I/O; concurrency with actual threads and race tests; filesystem/network boundaries and Unicode.
6. Expanded programming track: additional structures, complexity, debugger use, property-based testing, portable interfaces and deeper real Framework API exercises.
7. Additional Assembly targets and full Windows/RISC-V runtime evidence. Foreign object assembly alone is not runtime qualification.

## Capacity and preservation

The current learning plan has capacity 64; this catalogue has 53 entries. Before crossing that boundary, extend the existing course/plan model with explicit compatibility and migration rather than silently truncating the catalogue or creating an application-owned list. Preserve stable lesson IDs and existing progress.

## Scope of the books

The historical Bits to Banking volumes remain background material. New runnable lesson code must be checked against the current repository and labelled educational where it models a CPU, controller or queue rather than using a production Framework service. Do not copy old claimed test counts into current release evidence.
